import { ListingConfirmReservationResponseDto } from './listing-confirm-reservation-response-dto';

describe('ListingConfirmReservationResponseDto', () => {
  it('should create an instance', () => {
    expect(new ListingConfirmReservationResponseDto()).toBeTruthy();
  });
});
