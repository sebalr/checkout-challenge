import { ListingReservationCostRequestDto } from './listing-reservation-cost-request-dto';

describe('ListingReservationCostRequestDto', () => {
  it('should create an instance', () => {
    expect(new ListingReservationCostRequestDto()).toBeTruthy();
  });
});
