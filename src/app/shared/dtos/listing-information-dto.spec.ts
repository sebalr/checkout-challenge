import { ListingInformationDto } from './listing-information-dto';

describe('ListingInformationDto', () => {
  it('should create an instance', () => {
    expect(new ListingInformationDto()).toBeTruthy();
  });
});
